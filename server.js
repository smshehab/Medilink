require('dotenv').config();
const path = require('path');
const fs = require('fs');
const express = require('express');
const mysql = require('mysql2/promise');
const bcrypt = require('bcryptjs');
const jwt = require('jsonwebtoken');
const multer = require('multer');
const app = express();
const uploadDir = path.join(__dirname, 'uploads');
fs.mkdirSync(uploadDir, { recursive: true });
const upload = multer({ storage: multer.diskStorage({ destination: uploadDir, filename: (_, file, cb) => cb(null, `${Date.now()}-${file.originalname.replace(/[^a-zA-Z0-9._-]/g, '')}`) }), limits: { fileSize: 5 * 1024 * 1024 } });
const isLocalDatabase = !process.env.DB_HOST || ['localhost', '127.0.0.1'].includes(process.env.DB_HOST);
const pool = mysql.createPool({ host: process.env.DB_HOST || 'localhost', port: Number(process.env.DB_PORT || (isLocalDatabase ? 3306 : 4000)), user: process.env.DB_USER, password: process.env.DB_PASSWORD, database: process.env.DB_NAME || 'medilink_db', ssl: !isLocalDatabase && process.env.DB_SSL !== 'false' ? { rejectUnauthorized: true } : undefined, waitForConnections: true, connectTimeout: 15000, connectionLimit: 10 });
app.use(express.json()); app.use(express.static(__dirname)); app.use('/uploads', express.static(uploadDir));
const secret = process.env.JWT_SECRET || 'medilink-development-secret';
const auth = (...roles) => (req, res, next) => { try { const token = (req.headers.authorization || '').replace('Bearer ', ''); const user = jwt.verify(token, secret); if (roles.length && !roles.includes(user.role)) return res.status(403).json({ message: 'Access denied' }); req.user = user; next(); } catch { res.status(401).json({ message: 'Please log in first' }); } };
const pharmacyFor = async id => (await pool.query('SELECT id FROM pharmacies WHERE owner_id=? LIMIT 1', [id]))[0][0];
app.get('/api/health', async (_,res) => { try { await pool.query('SELECT 1'); res.json({ok:true,database:'connected'}); } catch (e) { console.error('DATABASE_CONNECTION_ERROR', e.code, e.message); res.status(503).json({ok:false,database:'unavailable'}); } });
app.post('/api/auth/register', async (req,res) => { try { const { fullName,email,phone,password,role='customer' }=req.body; if(!fullName||!email||!password) return res.status(400).json({message:'Name, email and password are required'}); const hash=await bcrypt.hash(password,10); const [r]=await pool.query('INSERT INTO users(full_name,email,phone,password_hash,role) VALUES(?,?,?,?,?)',[fullName,email,phone||null,hash,role==='pharmacist'?'pharmacist':'customer']); res.status(201).json({message:'Account created. You can log in now.',id:r.insertId}); } catch(e){ console.error('REGISTER_ERROR', e.code, e.message); res.status(e.code==='ER_DUP_ENTRY'?400:503).json({message:e.code==='ER_DUP_ENTRY'?'Email already registered':'Database is temporarily unavailable. Please try again shortly.'}); } });
app.post('/api/auth/login', async (req,res) => { try { const [rows]=await pool.query('SELECT * FROM users WHERE email=? AND active=1',[req.body.email]); const u=rows[0]; if(!u || !(await bcrypt.compare(req.body.password||'',u.password_hash))) return res.status(401).json({message:'Incorrect email or password'}); const token=jwt.sign({id:u.id,role:u.role,fullName:u.full_name},secret,{expiresIn:'7d'}); res.json({token,user:{id:u.id,fullName:u.full_name,role:u.role}}); } catch(e) { console.error('LOGIN_ERROR', e.code, e.message); res.status(503).json({message:'Database is temporarily unavailable. Please try again shortly.'}); } });
app.get('/api/medicines/search', async (req,res) => { try { const q=`%${req.query.q||''}%`; const [rows]=await pool.query(`SELECT m.id,m.brand_name,m.generic_name,m.unit_price,m.stock_qty,p.id pharmacy_id,p.name pharmacy,p.address,p.area FROM medicines m JOIN pharmacies p ON p.id=m.pharmacy_id WHERE p.approved=1 AND m.stock_qty>0 AND (m.brand_name LIKE ? OR m.generic_name LIKE ?) ORDER BY m.stock_qty DESC`,[q,q]); res.json(rows); } catch(e) { console.error('MEDICINE_SEARCH_ERROR', e.code, e.message); res.status(503).json({message:'Medicine database is temporarily unavailable.'}); } });
app.post('/api/reservations', auth('customer'), async (req,res) => { const {medicineId,pharmacyId,quantity}=req.body; const qty=Number(quantity); const [stock]=await pool.query('SELECT stock_qty FROM medicines WHERE id=? AND pharmacy_id=?',[medicineId,pharmacyId]); if(!stock[0]||stock[0].stock_qty<qty) return res.status(400).json({message:'Medicine is no longer available in this quantity'}); const code='ML'+Math.random().toString(36).slice(2,8).toUpperCase(); await pool.query('INSERT INTO reservations(customer_id,medicine_id,pharmacy_id,quantity,pickup_code) VALUES(?,?,?,?,?)',[req.user.id,medicineId,pharmacyId,qty,code]); res.status(201).json({message:'Reservation created',pickupCode:code}); });
app.get('/api/reservations/my',auth('customer'),async(req,res)=>{const [rows]=await pool.query(`SELECT r.*,m.brand_name,p.name pharmacy FROM reservations r JOIN medicines m ON m.id=r.medicine_id JOIN pharmacies p ON p.id=r.pharmacy_id WHERE r.customer_id=? ORDER BY r.created_at DESC`,[req.user.id]);res.json(rows)});
app.post('/api/emergency-requests',auth('customer'),async(req,res)=>{const {medicineName,message,area}=req.body;await pool.query('INSERT INTO emergency_requests(customer_id,medicine_name,message,area) VALUES(?,?,?,?)',[req.user.id,medicineName,message||null,area||null]);res.status(201).json({message:'Emergency request sent to nearby partner pharmacies'});});
app.post('/api/prescriptions',auth('customer'),upload.single('prescription'),async(req,res)=>{if(!req.file)return res.status(400).json({message:'Please choose a file'});await pool.query('INSERT INTO prescriptions(customer_id,file_path,note) VALUES(?,?,?)',[req.user.id,`/uploads/${req.file.filename}`,req.body.note||null]);res.status(201).json({message:'Prescription uploaded for verification'});});
app.post('/api/pharmacies',auth('pharmacist'),async(req,res)=>{const {name,address,area,phone}=req.body;await pool.query('INSERT INTO pharmacies(owner_id,name,address,area,phone) VALUES(?,?,?,?,?)',[req.user.id,name,address,area,phone]);res.status(201).json({message:'Pharmacy submitted for admin approval'});});
app.get('/api/pharmacy/dashboard',auth('pharmacist'),async(req,res)=>{const p=await pharmacyFor(req.user.id);if(!p)return res.json({needsPharmacy:true});const [[summary]]=await pool.query(`SELECT (SELECT COUNT(*) FROM medicines WHERE pharmacy_id=?) medicines,(SELECT COUNT(*) FROM medicines WHERE pharmacy_id=? AND stock_qty<=low_stock_level) lowStock,(SELECT COUNT(*) FROM medicines WHERE pharmacy_id=? AND expiry_date<=DATE_ADD(CURDATE(),INTERVAL 30 DAY)) expiring,(SELECT COALESCE(SUM(total_amount),0) FROM sales WHERE pharmacy_id=? AND DATE(sold_at)=CURDATE()) salesToday`,[p.id,p.id,p.id,p.id]);const [items]=await pool.query('SELECT * FROM medicines WHERE pharmacy_id=? ORDER BY expiry_date ASC',[p.id]);const [reservations]=await pool.query(`SELECT r.*,u.full_name,m.brand_name FROM reservations r JOIN users u ON u.id=r.customer_id JOIN medicines m ON m.id=r.medicine_id WHERE r.pharmacy_id=? AND r.status='pending'`,[p.id]);res.json({pharmacyId:p.id,summary,items,reservations});});
app.post('/api/medicines',auth('pharmacist'),async(req,res)=>{const p=await pharmacyFor(req.user.id);if(!p)return res.status(400).json({message:'Create your pharmacy first'});const {brandName,genericName,category,unitPrice,stockQty,lowStockLevel,expiryDate,supplierId}=req.body;await pool.query('INSERT INTO medicines(pharmacy_id,supplier_id,brand_name,generic_name,category,unit_price,stock_qty,low_stock_level,expiry_date) VALUES(?,?,?,?,?,?,?,?,?)',[p.id,supplierId||null,brandName,genericName||null,category||null,unitPrice,stockQty,lowStockLevel||10,expiryDate||null]);res.status(201).json({message:'Medicine added to inventory'});});
app.post('/api/sales',auth('pharmacist'),async(req,res)=>{const p=await pharmacyFor(req.user.id);const {medicineId,quantity}=req.body;const [[m]]=await pool.query('SELECT unit_price,stock_qty FROM medicines WHERE id=? AND pharmacy_id=?',[medicineId,p.id]);if(!m||m.stock_qty<quantity)return res.status(400).json({message:'Insufficient stock'});await pool.query('UPDATE medicines SET stock_qty=stock_qty-? WHERE id=?',[quantity,medicineId]);await pool.query('INSERT INTO sales(pharmacy_id,medicine_id,quantity,total_amount) VALUES(?,?,?,?)',[p.id,medicineId,quantity,m.unit_price*quantity]);res.json({message:'Sale recorded'});});
app.get('/api/admin/overview',auth('admin'),async(req,res)=>{const [[data]]=await pool.query(`SELECT (SELECT COUNT(*) FROM users) users,(SELECT COUNT(*) FROM pharmacies WHERE approved=1) pharmacies,(SELECT COUNT(*) FROM medicines) medicines,(SELECT COUNT(*) FROM emergency_requests WHERE status='open') emergencies`);const [pending]=await pool.query('SELECT p.*,u.full_name owner FROM pharmacies p LEFT JOIN users u ON u.id=p.owner_id WHERE p.approved=0');res.json({data,pending});});
app.patch('/api/admin/pharmacies/:id/approve',auth('admin'),async(req,res)=>{await pool.query('UPDATE pharmacies SET approved=1 WHERE id=?',[req.params.id]);res.json({message:'Pharmacy approved'});});
app.get('*',(_,res)=>res.sendFile(path.join(__dirname,'index.html')));
app.listen(process.env.PORT||3000, '0.0.0.0', ()=>console.log(`MediLink running at http://localhost:${process.env.PORT||3000}`));
